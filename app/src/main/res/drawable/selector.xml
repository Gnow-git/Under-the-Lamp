<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 선택 시 특정 색으로 -->
    <item android:color="@color/selectColor" android:state_checked="true" />

    <!-- 선택 되지 않았을 때 -->
    <item android:color="@color/defaultColor" android:state_checked="false" />
</selector>